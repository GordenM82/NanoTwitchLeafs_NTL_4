<Application x:Class="NanoTwitchLeafs.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Startup="App_Startup"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <SolidColorBrush x:Key="NtlBackgroundBrush" Color="#0F141C" />
        <SolidColorBrush x:Key="NtlSurfaceBrush" Color="#171D27" />
        <SolidColorBrush x:Key="NtlSurfaceRaisedBrush" Color="#202735" />
        <SolidColorBrush x:Key="NtlBorderBrush" Color="#343D4C" />
        <SolidColorBrush x:Key="NtlTextBrush" Color="#F4F6FA" />
        <SolidColorBrush x:Key="NtlMutedTextBrush" Color="#AEB7C6" />
        <SolidColorBrush x:Key="NtlAccentBrush" Color="#8B5CF6" />
        <SolidColorBrush x:Key="NtlAccentHoverBrush" Color="#9D75F8" />
        <SolidColorBrush x:Key="NtlSuccessBrush" Color="#66D17A" />

        <Style TargetType="Window">
            <Setter Property="Background" Value="{StaticResource NtlBackgroundBrush}" />
            <Setter Property="Foreground" Value="{StaticResource NtlTextBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
        </Style>

        <Style TargetType="Label">
            <Setter Property="Foreground" Value="{StaticResource NtlTextBrush}" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource NtlTextBrush}" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource NtlTextBrush}" />
            <Setter Property="Background" Value="{StaticResource NtlSurfaceRaisedBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource NtlBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,7" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#2A3342" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{StaticResource NtlAccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#33235C" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource NtlTextBrush}" />
            <Setter Property="Background" Value="#111721" />
            <Setter Property="BorderBrush" Value="{StaticResource NtlBorderBrush}" />
            <Setter Property="CaretBrush" Value="{StaticResource NtlTextBrush}" />
            <Setter Property="Padding" Value="8,5" />
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Foreground" Value="{StaticResource NtlTextBrush}" />
            <Setter Property="Background" Value="#111721" />
            <Setter Property="BorderBrush" Value="{StaticResource NtlBorderBrush}" />
            <Setter Property="Padding" Value="8,5" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="#10141B" />
            <Setter Property="Background" Value="{StaticResource NtlSurfaceRaisedBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource NtlBorderBrush}" />
            <Setter Property="Padding" Value="7,4" />
            <Setter Property="MinHeight" Value="32" />
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource NtlTextBrush}" />
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Foreground" Value="{StaticResource NtlTextBrush}" />
            <Setter Property="Background" Value="#111721" />
            <Setter Property="BorderBrush" Value="{StaticResource NtlBorderBrush}" />
            <Setter Property="Padding" Value="5" />
        </Style>

        <Style TargetType="TabControl">
            <Setter Property="Foreground" Value="{StaticResource NtlTextBrush}" />
            <Setter Property="Background" Value="{StaticResource NtlSurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource NtlBorderBrush}" />
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="{StaticResource NtlMutedTextBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Margin" Value="2,0" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="Background" Value="#3C2A65" />
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="White" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Separator">
            <Setter Property="Background" Value="{StaticResource NtlBorderBrush}" />
        </Style>
    </Application.Resources>
</Application>
