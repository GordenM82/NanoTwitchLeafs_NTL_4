<Window x:Class="NanoTwitchLeafs.Windows.DeviceGroupsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nanoleaf-Gerätegruppen" Height="430" Width="620" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="12" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="38" />
        </Grid.RowDefinitions>

        <GroupBox Header="Gruppen" Grid.Column="0">
            <ListBox x:Name="Groups_ListBox" SelectionChanged="Groups_ListBox_SelectionChanged" DisplayMemberPath="Name" />
        </GroupBox>

        <GroupBox Header="Gruppe bearbeiten" Grid.Column="2">
            <DockPanel Margin="6">
                <StackPanel DockPanel.Dock="Top">
                    <Label Content="Name" Padding="0,0,0,3" />
                    <TextBox x:Name="GroupName_TextBox" Height="24" Margin="0,0,0,8" />
                    <Label Content="Nanoleafs in dieser Gruppe" Padding="0,0,0,3" />
                </StackPanel>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="GroupDevices_ItemsControl" />
                </ScrollViewer>
            </DockPanel>
        </GroupBox>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Neue Gruppe" Click="NewGroup_Button_Click" Padding="8,2" Margin="4,0" />
            <Button Content="Gruppe speichern" Click="SaveGroup_Button_Click" Padding="8,2" Margin="4,0" />
            <Button Content="Gruppe löschen" Click="DeleteGroup_Button_Click" Padding="8,2" Margin="4,0" />
            <Button Content="Schließen" Click="Close_Button_Click" Padding="8,2" Margin="4,0" />
        </StackPanel>
    </Grid>
</Window>
