<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:p="clr-namespace:NanoTwitchLeafs.Properties"
    x:Name="Trigger_Window" x:Class="NanoTwitchLeafs.Windows.TriggerWindow"
        mc:Ignorable="d"
        Title="{x:Static p:Resources.Window_Trigger_Title}" Height="760" Width="1500" MinHeight="560" MinWidth="1000" ResizeMode="CanResizeWithGrip" WindowStartupLocation="CenterOwner" Background="{DynamicResource NtlBackgroundBrush}" Foreground="{DynamicResource NtlTextBrush}">
    <Grid>
        <Grid.Resources>
            <Style TargetType="ListViewItem">
                <Setter Property="Foreground" Value="{DynamicResource NtlTextBrush}" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ListViewItem"><Border x:Name="row" Background="{TemplateBinding Background}" BorderBrush="Transparent" BorderThickness="1" Padding="2"><GridViewRowPresenter Content="{TemplateBinding Content}" Columns="{Binding View.Columns, RelativeSource={RelativeSource AncestorType={x:Type ListView}}}" VerticalAlignment="Center"/></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="row" Property="BorderBrush" Value="{DynamicResource NtlAccentBrush}"/></Trigger><Trigger Property="IsSelected" Value="True"><Setter TargetName="row" Property="Background" Value="{DynamicResource NtlAccentSurfaceBrush}"/><Setter TargetName="row" Property="BorderBrush" Value="{DynamicResource NtlAccentBrush}"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter>
                <Style.Triggers>
                    <Trigger Property="ItemsControl.AlternationIndex"  Value="0">
                        <Setter Property="Background" Value="{DynamicResource NtlSurfaceBrush}" />
                    </Trigger>
                    <Trigger Property="ItemsControl.AlternationIndex"  Value="1">
                        <Setter Property="Background" Value="{DynamicResource NtlSurfaceRaisedBrush}" />
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="{DynamicResource NtlAccentSurfaceBrush}"/><Setter Property="Foreground" Value="{DynamicResource NtlTextBrush}"/></Trigger>
                </Style.Triggers>
            </Style>
        </Grid.Resources>
        <ListView x:Name="Trigger_Listview" AlternationCount="2" Margin="6,6,6,58" HorizontalAlignment="Stretch" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="{x:Static p:Resources.Window_Trigger_Header_On}" Width="46">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Slider  Value="{Binding OnOffSliderValue}" Background="{Binding OnOffSliderBackground}" Tag="{Binding OnOffSliderTag}" Width="30" HorizontalAlignment="Center" SmallChange="1" Minimum="0" Maximum="1" ValueChanged="OnOffSlider_ValueChanged" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="{x:Static p:Resources.Window_Trigger_Header_Trigger}" Width="100" DisplayMemberBinding="{Binding Trigger}" />
                    <GridViewColumn Header="{x:Static p:Resources.Window_Trigger_Header_Command}" Width="120" DisplayMemberBinding="{Binding Command}" />
                    <GridViewColumn Header="{x:Static p:Resources.Window_Trigger_Header_Effect}" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Label x:Name="EffectName_Label" Content="{Binding Effect}" Background="{Binding Background}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="96" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="{x:Static p:Resources.Window_Trigger_Header_Sound}" Width="100" DisplayMemberBinding="{Binding Sound}" />
                    <GridViewColumn Header="{x:Static p:Resources.Window_Trigger_Header_Duration}" Width="80" DisplayMemberBinding="{Binding Duration}" />
                    <GridViewColumn Header="{x:Static p:Resources.Window_Trigger_Header_Brightness}" Width="80" DisplayMemberBinding="{Binding Brightness}" />
                    <GridViewColumn Header="{x:Static p:Resources.Window_Trigger_Header_Amount}" Width="60" DisplayMemberBinding="{Binding Amount}" />
                    <GridViewColumn Header="{x:Static p:Resources.Window_Trigger_Header_Cooldown}" Width="80" DisplayMemberBinding="{Binding Cooldown}" />
                    <GridViewColumn Header="{x:Static p:Resources.Window_Trigger_Header_Flags}" Width="130" DisplayMemberBinding="{Binding VipSubMod}" />
                    <GridViewColumn Header="Zielgeräte" Width="140" DisplayMemberBinding="{Binding TargetDevices}" />
                    <GridViewColumn Header="" Width="62">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Width="48" HorizontalAlignment="Center" Content="{x:Static p:Resources.Window_Trigger_Button_Test}" Click="TestButton_Click" HorizontalContentAlignment="Center" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="" Width="92">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Width="78" HorizontalAlignment="Center" Content="{Binding DuplicateText}" Click="DuplicateButton_Click" HorizontalContentAlignment="Center" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="" Width="62">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Width="48" HorizontalAlignment="Center" Content="{x:Static p:Resources.Window_Trigger_Button_Edit}" Click="EditButton_Click" HorizontalContentAlignment="Center" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="" Width="62">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Width="48" HorizontalAlignment="Center" Content="{x:Static p:Resources.Window_Trigger_Button_Delete}" Click="DeleteButton_Click" HorizontalContentAlignment="Center" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        <Grid VerticalAlignment="Bottom" Margin="10"><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <Button x:Name="newCmd_Button" Content="{x:Static p:Resources.Window_Trigger_Button_New}" Width="130" Click="NewCmd_Button_Click" Margin="0,0,8,0" />
            <Button x:Name="importCmd_Button" Grid.Column="1" Content="Importieren" Width="130" Click="ImportCmd_Button_Click" Margin="0,0,8,0" />
            <Button x:Name="exportCmd_Button" Grid.Column="2" Content="Exportieren" Width="130" Click="ExportCmd_Button_Click" />
            <Button x:Name="clearCmd_Button" Grid.Column="4" Content="{x:Static p:Resources.Window_Trigger_Button_Clear}" Width="130" Click="ClearCmd_Button_Click" />
        </Grid>
    </Grid>
</Window>
