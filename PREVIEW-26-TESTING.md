# Preview 26 test checklist

- Switch between Light, Dark and System themes; check scrollbars, console levels, context menus and unsaved-change warnings.
- In the console, verify that Automatic scrolling and the result count are vertically aligned with the buttons.
- At the standard window size, the Twitch page should open without the tiny initial vertical scroll range.
- The trigger list keeps its existing table layout but should fit substantially better without horizontal scrolling at the standard size.
- The information-page title, version and developer block are centered and visually balanced against the logo.
- The user already confirmed the Preview 24/25 functional regression test; it is not repeated as a manual P26 task.
